# 리팩토링 분석 — 죽은 코드 + 구조 위반 + 운영 전 잔여

조사일: 2026-05-05
범위: `front/` 전체 (components / pages / lib / routes / data / vite.config)
규칙 기준: `CLAUDE.md`, `agents/frontend/component-placement.md`, `agents/frontend/admin-inline-editing-pattern.md`
모드: **분석만, 코드 수정 X.**

---

## 0. 한 장 요약

치명적 죽은 코드는 거의 없다. **구조 위반 1건** (`pages/` 컴포넌트를 `components/` 가 import) 과 **고아 페이지 1건** (`Preview.jsx`) 이 가장 큰 이슈. PLACEHOLDER 데이터 잔여는 운영 전 교체 체크리스트로 따로 관리.

---

## 1. 구조 위반 (P1 — 우선 정리)

### 1-1. `components/` 가 `pages/` 컴포넌트를 직접 import (계층 역전)

| 파일 | 위반 import | 영향 |
|------|-------------|------|
| `front/src/components/HomeClient.jsx` | `pages/guide/oneday/MiniHompy`, `MiniHompyLive` | 컴포넌트 → 페이지 역방향 의존, 페이지 이동 시 깨짐 |
| `front/src/components/GuideClient.jsx` | `pages/guide/oneday/MiniHompy` | 같음 |
| `front/src/components/MiniHompyGuide.jsx` | `pages/guide/oneday/MiniHompyLive` | 같음 |

**원인**: `MiniHompy` / `MiniHompyLive` 가 라우트(페이지)이면서 동시에 라이브 데모 컴포넌트(EventDetail 배너, 홈 데모 영역)로 재사용됨. 두 역할 분리 안 됨.

**권장 정리**:
- `pages/guide/oneday/MiniHompy.jsx` → `components/demo/MiniHompy.jsx` 로 이동
- `pages/guide/oneday/MiniHompy.jsx` 는 새 위치에서 import 해 라우트 래퍼 한 줄 (`export default MiniHompy`) 만 남김
- `MiniHompyLive` 동일
- `agents/frontend/component-placement.md` 에 "라이브 데모 컴포넌트는 pages 가 아니라 components/demo/ 에 둔다" 룰 추가

### 1-2. `EventDetail.jsx` 의 `<dl>` 정보 박스와 `ClassRegistration.DateVenueBox` 정보 중복 — **방금 정리 완료 ✓**

(2026-05-05 작업: `EventDetail` 의 dl 통째 제거 + `ClassRegistration` 가드 완화로 자체 강의 메타 단일 소스화. 본 항목은 처리됨, 기록만 남김.)

---

## 2. 고아 페이지 / 미등록 라우트 (P2)

| 파일 | 상태 | 조치 |
|------|------|------|
| `front/src/pages/guide/oneday/Preview.jsx` | 라우트 미등록 (`routes/guide.jsx` 에 `/guide/oneday/preview` 없음) | 가이드 본문에서 링크 박혀있는지 확인 → 사용 중이면 라우트 등록, 안 쓰면 삭제 |

---

## 3. 데이터 일관성 / PLACEHOLDER 잔여 (P3 — 운영 전 체크리스트)

| 파일 | 항목 | 현재 값 | 액션 |
|------|------|---------|------|
| `front/public/data/site.json` | `kakaoOpenChatUrl` | `https://open.kakao.com/o/PLACEHOLDER` | 실제 카톡 오픈채팅 URL 로 교체 (operator.contacts.kakao 와 통합 검토) |
| `front/public/data/site.json` | `instagramUrl` | `https://instagram.com/PLACEHOLDER` | 실제 인스타 또는 제거 (operator.contacts.instagram 과 통합 검토) |
| `front/public/data/site.json` | `danggeunUrl` | `https://www.daangn.com/PLACEHOLDER` | 당근 모임 페이지 URL 또는 제거 |
| `front/public/data/events.json` | `evt-week1-2026-05-10.payment.bank` | `"TODO 은행명 입력"` | 운영자 본인 은행 입력 (admin 편집 또는 직접) |
| `front/public/data/events.json` | `evt-week1-2026-05-10.payment.account` | `"TODO 계좌번호 입력"` | 운영자 본인 계좌 입력 |
| `front/public/data/events.json` | 외부 이벤트 11건 (evt-101 ~ evt-111) | 모두 `isPublished: false` | **의도 확인 필요.** 외부 이벤트도 1차 노출 대상이면 publish, 데모용이면 유지 |

**중복 가능성**:
- `site.kakaoOpenChatUrl` 과 `site.operator.contacts.kakao` 가 둘 다 카톡 URL — 한 곳으로 통합 권장 (`operator.contacts.kakao` 가 더 의미 명확)
- `site.instagramUrl` 과 `site.operator.contacts.instagram` 동일

---

## 4. CLAUDE.md 룰 준수 (정상)

- ✓ Tailwind `gray-*` 0건, `slate-*` 361건 (1-4 컬러 룰 OK)
- ✓ `app.jsx` lazy loading 패턴 정상 (모든 페이지 lazy import)
- ✓ `useSyncExternalStore` 기반 단일 소스 패턴 (`useOperator`, `useEvents`) 일관
- ✓ Vite dev 미들웨어 `apply: 'serve'` 가드 (배포 산출물 분리)

---

## 5. agents/ 문서 등록 상태 (정상)

| 신규 패턴 | 등록 위치 |
|----------|----------|
| Curriculum-Class 패턴 | `agents/frontend/class-registration-pattern.md` ✓ |
| Admin 인라인 편집 | `agents/frontend/admin-inline-editing-pattern.md` ✓ |
| 운영자 프로필 모듈 | `agents/ui/operator-profile-module.md` ✓ |

신규 추가/이동 권장 (위 § 1-1 정리 시):
- `agents/frontend/component-placement.md` 에 "라이브 데모 컴포넌트는 components/demo/" 룰 추가
- 또는 새 문서 `agents/frontend/live-demo-component-pattern.md`

---

## 6. 추가 관찰 (정보성)

### 6-1. `OnedayClassCurriculum` 의 curriculumId 무관성
- 현재 `EventDetail.jsx` 의 `showCurriculum = isInternal && type === 'oneday_class'` → `OnedayClassCurriculum` 무조건 렌더
- 그러나 `OnedayClassCurriculum` 자체는 1주차 콘텐츠 하드코딩
- 같은 교안의 N회차는 OK 지만, **2주차·3주차 교안 추가 시** 매핑 필요
- 이미 `agents/frontend/class-registration-pattern.md` 에서 향후 작업으로 명시됨 (옵션 B/C)

### 6-2. `back/` 디렉터리
- 본 분석 범위 외(`front/` 만 조사). 향후 백엔드 도입 트리거(Phase 1+) 도달 시 별도 분석 필요.

### 6-3. `docs/plan/2026-05-04/2230_*.md` 의 § 4 구현 순서
- Step 1~5 모두 완료(Curriculum-Class 패턴 + ClassRegistration + EventDetail 통합 + 정원 수동 갱신). 본 분석 결과 반영 후 해당 플랜의 체크리스트 닫아도 OK.

---

## 7. 우선순위 액션 요약

| 우선 | 항목 | 추정 작업량 |
|------|------|------------|
| P1 | `MiniHompy` / `MiniHompyLive` 를 `components/demo/` 로 이동 + 라우트 래퍼 분리 | 30분 |
| P2 | `Preview.jsx` 사용처 확인 → 라우트 등록 또는 삭제 | 10분 |
| P3 | `site.json` 의 PLACEHOLDER URL 교체 + `operator.contacts.*` 와 통합 | 5분 (데이터만) |
| P3 | `evt-week1-2026-05-10` payment 의 TODO 마커 채우기 | 1분 (운영자 결정) |
| P4 | 외부 이벤트 publish 의도 결정 → events.json 일괄 갱신 | 5분 |
| P4 | `agents/frontend/component-placement.md` 에 라이브 데모 룰 추가 | 5분 |

**총 50분 이내**로 끝낼 수 있는 분량. 한 번에 다 하지 말고 P1 → P2 → P3 순으로 끊어서.

---

## 8. 안 한 것 / 후속 분석 후보

- 백엔드(`back/`) 정리 — Phase 1+ 도입 시
- `agents/`·`docs/` 자체의 죽은 문서 식별 — 별도 작업 (문서가 코드보다 자주 stale)
- 빌드 산출물 크기 분석 (불필요한 lib 동봉 여부)
- 모바일 360~430px 폭에서 ClassRegistration / Admin 페이지 시각 검수 (UI QA)
