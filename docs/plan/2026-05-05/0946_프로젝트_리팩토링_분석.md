# 바이브 세션 — 프로젝트 리팩토링 분석 (2026-05-05)

분석 범위: `front/src` + `front/public/data` + `back/` + `agents/` + `CLAUDE.md`
조사 기준: import / 동적 import / route element / Link / Tailwind class / JSON 필드 사용처 grep

> **이 문서는 분석 전용. 코드 수정·삭제 액션 0건. 모든 항목은 grep 카운트 기반 근거 제시.**
> 글로벌 룰 준수: "Empty data does not automatically mean dead structure." → 의심만 가는 항목은 "보존 권장"으로 분류.

---

## 요약

| 분류 | 건수 | 비고 |
|------|-----|------|
| 🔴 죽은 코드 / 미참조 | 5 | route 누락 1, 미사용 export 2, 미정의 CSS 클래스 3, 데드 링크 1, 더미 backend 코드 묶음 |
| 🟠 중복 / 통합 후보 | 3 | PromptRef 2회 정의, GitHubGuide/MiniHompyGuide/VercelGuide 폼팩터 중복, useEvents/useOperator 동형 패턴 |
| 🟡 책임 비대 / 분리 후보 | 4 | HomeClient 624줄, OnedayClassCurriculum 375줄, ClassEditor 348줄, GuideClient 492줄 |
| 🟣 위치 위반 | 1 | (큰 위반 없음) MiniHompyGuide/GitHubGuide/VercelGuide의 "도메인 그룹" 미분류 |
| 🟢 데이터/스키마 정합성 | 6 | curriculum 7필드 0회 참조, site.json 5필드 0회, JSON 4종 0회 import, Home.jsx prop 이름 불일치 (실 버그) |
| ⚪ 문서 ↔ 코드 어긋남 | 5 | OperatorAboutHero(존재 안 함), showAntiPlagiarismNotice prop(미구현), html.light vs html.dark, server.py 경로, agents 인덱스 누락 |
| 🔵 백엔드 부분 | 1 | Django 코드는 작성됐으나 마이그레이션 0개 + 프론트 client/* 0회 호출 → Phase 2 미연결 placeholder |

### 우선 처리 권장 Top 5 (위험도 + 효과 기준)

| 순위 | 항목 | 이유 |
|-----|------|-----|
| 1 | **`Home.jsx` prop 이름 불일치 (`site={siteData}` → HomeClient는 `kakaoOpenChatUrl` 받음)** | **실 버그**. 카톡 오픈채팅 링크 5군데가 `href={undefined}` → "#"으로 깨져 동작. 위험도 high. 1줄 수정. |
| 2 | guide/index.jsx의 데드 링크 3개 (`/guide/oneday/preview`, `/guide/beginner`, `/guide/claude`) — 라우트 미등록 | 페이지 파일은 다 있는데 routes/guide.jsx에 정의 0개. "강의 맛보기 →" 클릭 시 빈 화면. 위험도 med. routes에 3줄 추가 or 링크 비활성화. |
| 3 | HomeClient `<Link to="/library">` 데드 링크 | `/library` 라우트 0개. 클릭하면 빈 화면. 위험도 med. CTA 버튼 자체 제거 또는 라우트 추가 결정 필요. |
| 4 | `agents/ui/operator-profile-module.md` ↔ 실제 코드 — `OperatorAboutHero` 컴포넌트는 문서가 가정만 하고 파일/사용처 0개. `showAntiPlagiarismNotice` prop도 0회 구현. | AI 작업 시 잘못된 문서 따라 가짜 컴포넌트 import 시도 → 빌드 실패. 위험도 med. 문서 갱신 필요. |
| 5 | `globals.css` 미정의 클래스 `btn`, `btn-ghost`, `section-title` 사용 (총 6회) | Tailwind는 모르는 클래스 무시. 의도된 스타일이 적용 안 됨 → 후기 섹션 제목, CTA 버튼, 캘린더 좌우 화살표가 디자인 의도와 다르게 보임. 위험도 low~med. globals.css에 3줄 추가 or 호출부에서 Tailwind 클래스로 교체. |

---

## 1. 🔴 죽은 코드 / 미참조

### 1-1. `front/src/routes/guide.jsx` — `/guide/oneday/preview`, `/guide/beginner`, `/guide/claude` 라우트 누락
- 증상: `pages/guide/oneday/Preview.jsx`(112줄), `pages/guide/beginner/index.jsx`(24줄), `pages/guide/claude/index.jsx`(24줄) 파일은 존재하는데 routes/guide.jsx에서 import 0회. 동시에 `pages/guide/index.jsx`는 이 3경로로 Link 또는 SECTIONS 정의 중 (`active: true`로 표시).
- 근거: `Grep "Preview" front/src` → routes/index에 없음. `Grep "guide/beginner|guide/claude|guide/oneday/preview" front/src` → 7건 모두 표시·링크용, 라우트 정의 0건.
- 권장: routes/guide.jsx 에 3개 라우트 추가 (3줄) — Preview/beginner/claude 모두 별도 코드가 살아있음. **삭제 X**, 라우트 등록만.
- 위험도: **med** (사용자 클릭 시 빈 화면)
- 영향: 가이드 진입 사용자 + SEO. 모든 페이지 정상 import 됨, 라우트만 추가하면 동작.

### 1-2. `front/src/components/AdminOnly.jsx` — `AdminOnly` (default), `DevOnly` (named) 미사용
- 증상: `AdminOnly` (default export, role==='admin' 가드) — 호출부 0회. `DevOnly` (named export, dev 환경 가드) — 호출부 0회. 사용 중인 것은 `AdminDevOnly` 단 하나.
- 근거: `Grep "AdminDevOnly" -> 7회 / "AdminOnly\b"(보호 장치 가드, 단어 경계) -> 정의·문서만 / "DevOnly" -> 정의 1회만`.
- 권장: **보존**. 의도적 인터페이스(Phase 1+ 백엔드 인증 도입 시 `AdminOnly` 활성화 예정, agents 문서에서 명시). 글로벌 룰의 "future placeholder 보존" 원칙. 미래 사용처 명시한 주석 추가 정도만.
- 위험도: low
- 영향: 0 (사용처 없음)

### 1-3. `front/src/lib/format.js` — `formatDate`, `libraryFormatLabel`, `libraryAccessLabel` 0회 호출
- 증상: 3개 export 모두 호출부 0회. `library*` 두 함수는 `library.json` (존재 X) 컨텍스트.
- 근거: `Grep "\bformatDate\b" -> 0 hits` (formatDateTime은 사용중). `Grep "libraryFormatLabel|libraryAccessLabel" -> docs만 hit, src 0회`.
- 권장: **보존**. 라이브러리 기능은 docs/plan과 README에 명시된 향후 도입 기능. `formatDate` 도 12자 짜리 단일 함수라 비용 0. 단, library 기능 도입 보류 결정시 함수 제거 가능.
- 위험도: low
- 영향: 번들 크기 ~150 bytes

### 1-4. `front/src/client/{auth,events,community,index}.js` 4개 파일 모두 호출부 0회
- 증상: `authClient`, `eventsClient`, `communityClient`, `axios api` instance 모두 import·call 0회. axios 의존성도 사실상 미사용.
- 근거: `Grep "authClient|eventsClient|communityClient|from '@/client|from.*/client/(auth|events|community|index)" -> 정의 3건 외 호출 0건`.
- 권장: **보존**. Phase 2 백엔드 연결용 placeholder. `apps/{accounts,events,community,library}/views.py`와 1:1 매핑. `back/`이 Phase 2에 진짜 연결되면 호출부가 한꺼번에 생김. 글로벌 룰 — placeholder는 제거 X.
- 위험도: low
- 영향: 번들 +13kb axios. dynamic import 안 하면 dead-code-elimination 안 됨. 단 lazy 페이지가 import 안 하므로 트리쉐이킹 가능.

### 1-5. `front/src/components/HomeClient.jsx:360` — `<Link to="/library">` 데드 라우트
- 증상: `/library` 라우트 정의 0개. CTA "지식 보러 가기" 버튼이 빈 페이지로 보냄.
- 근거: `Grep "/library" front/src -> 1 hit (HomeClient line 360 only)`. routes/* 어디에도 정의 없음. `library.json` 도 없음.
- 권장: 두 옵션 중 결정 필요 — (A) 버튼을 `/guide`로 변경 (현 가이드 페이지가 "지식" 역할), (B) 향후 라이브러리 기능 활성 시까지 버튼 자체 제거.
- 위험도: med
- 영향: CTA 클릭 사용자 (홈 하단)

---

## 2. 🟠 중복 / 통합 후보

### 2-1. `PromptRef` 컴포넌트 정의 2회 — `MiniHompyGuide.jsx:186` + `VercelGuide.jsx:265`
- 증상: 동일 컴포넌트(props·UI·동작 모두 일치)가 두 파일에 그대로 복붙. 49줄 × 2 = 98줄 중복.
- 근거: `Grep "function PromptRef|const PromptRef" -> 2건 (정의), 2건 (호출)`. 코드 diff 비교 — 동일.
- 권장: `components/PromptRef.jsx`로 추출 후 두 파일에서 import. 또는 `components/lecture/PromptRef.jsx`처럼 도메인 폴더로 묶기 (4번 위치 위반과 함께). 단순 추출은 위험 0.
- 위험도: low
- 영향: -49줄, 향후 새 Chapter 컴포넌트(예: Week2Guide) 추가 시 중복 방지

### 2-2. `GitHubGuide.jsx` + `MiniHompyGuide.jsx` + `VercelGuide.jsx` — Chapter 폼팩터 거의 동일
- 증상: 3개 파일 모두 동일 패턴: `SECTIONS = [{label, badge, desc, steps:[{num, color, title, desc, detail, img, prompt, tip}]}]` → 같은 SECTIONS.map → step.map 렌더 + `isAdmin` 토글. 차이는 데이터(SECTIONS)와 일부 옵셔널 박스(VercelGuide의 핑크 차별 포인트, GitHubGuide의 warnBox).
- 근거: 3개 파일 라인 수 (237 + 212 + 291) 중 ~70%가 동일 구조. CSS Module도 셋이 `GitHubGuide.module.css` 하나를 공유.
- 권장: `components/lecture/ChapterGuide.jsx` 1개로 통합 후 SECTIONS 데이터를 prop or 별도 데이터 파일(`data/chapters/*.js`)로 외부화. **단, 우선순위는 낮춤** — 현재 3개 운용 중이고 새 Chapter 추가가 활발한 시기라 데이터 위치 변경이 작업 흐름 깰 수 있음. agents/ui/lecture-guide-component.md가 이 패턴을 표준으로 명시. 새 Chapter 4개 이상 만들 때 통합 권장.
- 위험도: med (대형 리팩터)
- 영향: -400줄 가능, 신규 Chapter 추가 비용 절반 ↓

### 2-3. `lib/useEvents.js` + `lib/useOperator.js` — 동형 패턴
- 증상: `useSyncExternalStore` + localStorage 오버라이드 + commit/clear/notify의 동일 구조. ~40줄짜리 동일 보일러플레이트.
- 근거: 두 파일을 옆에 놓고 보면 변수명만 다른 같은 패턴. agents/frontend/admin-inline-editing-pattern.md도 "동형 패턴"으로 명시.
- 권장: **유지**. 미래 데이터 단일 소스(curriculums, members, ...)가 같은 패턴으로 1개 더 생기면 그때 `lib/createOverrideStore(KEY, seed)` 팩토리로 추출. 현재는 2개라 통합 비용 > 이득.
- 위험도: low
- 영향: -30줄 가능, 단 두 곳에만 적용되므로 추상화 ROI 낮음

---

## 3. 🟡 책임 비대 / 분리 후보

### 3-1. `front/src/components/HomeClient.jsx` — 624줄, 책임 7개 이상
- 증상: 한 파일에 (1) 필터 상태 5개 + (2) 날짜 필터 + (3) source 토글 + (4) Accordion 지도 + (5) 카드 그리드 + (6) 추천 카드 + (7) 캘린더 모달 + (8) 후기 + (9) CTA. `useState` 8개, 로컬 서브컴포넌트 8개 (`FilterRow`, `MapLegend`, `GuidePreviewCard`, `RecommendedHero`, `CardsGrid`, `NewClassCard`, `BeginnerGuide`, `CalendarView`).
- 근거: `wc -l HomeClient.jsx` → 624. `Grep "useState" -> 8 hits in this file`.
- 권장: 분리 단계 (난이도 순):
  1. **CalendarView (~60줄)** → `components/CalendarView.jsx` (독립 데이터 / 단일 prop). 가장 안전.
  2. **GuidePreviewCard + RecommendedHero (~80줄)** → `components/home/HomeHeroCards.jsx`.
  3. **CardsGrid + NewClassCard (~50줄)** → `components/home/CardsGrid.jsx`.
  4. **필터 상태 (5 useState + filtered useMemo)** → `lib/useEventFilters.js` 커스텀 훅.
  HomeClient는 조립만 담당하는 200줄 이하로.
- 위험도: med (분리 도중 prop drilling 실수 가능)
- 영향: 가독성·재사용성 큰 개선. CalendarView는 /admin·/me에서도 향후 재사용 가능.

### 3-2. `front/src/components/OnedayClassCurriculum.jsx` — 375줄, 4개 서브컴포넌트
- 증상: TIMELINE 데이터 (102줄) + PrologueTimeline + ClassPreview + PrepSection + ChapterAccordion 모두 한 파일. ChapterAccordion 안에서 GitHubGuide/MiniHompyGuide/VercelGuide 동적 매핑.
- 근거: `wc -l OnedayClassCurriculum.jsx` → 375.
- 권장: TIMELINE/PREP/PREVIEW_SHOTS 데이터 → `front/public/data/curriculum-week1-timeline.json` 으로 추출 (curriculum 도메인 데이터 일관성). ChapterAccordion 만 별도 컴포넌트로. 단 우선순위 낮음 — 작동 안정적이고 1주차에 한정.
- 위험도: low
- 영향: -100줄, 향후 주차별 timeline 데이터 추가 시 일관성

### 3-3. `front/src/components/ClassEditor.jsx` — 348줄
- 증상: 폼 입력 30개 + state 4개 + handler 5개. `Section` `Field` 헬퍼는 같은 파일 내 정의.
- 근거: `wc -l ClassEditor.jsx` → 348.
- 권장: **유지**. 단일 모달의 책임이 명확. 분리해도 7개 박스가 한 모달에서 함께 의미를 가짐(class-registration-pattern.md와 같은 논리). props drilling만 늘어남.
- 위험도: low
- 영향: -

### 3-4. `front/src/components/GuideClient.jsx` — 492줄
- 증상: STEPS_COMMON / STEPS_CLAUDE_EXT / STEPS_CODEX_EXT / STEPS_CLAUDE / STEPS_CODEX 데이터 (~120줄) + visual 스크린샷 컴포넌트 (~150줄) + 본문 렌더 (~200줄).
- 근거: `wc -l GuideClient.jsx` → 492.
- 권장: STEPS 데이터 → `front/src/pages/guide/oneday/install-steps.js` 로 분리. 시각 컴포넌트(VSCodeExtReal 등) → 같은 폴더에 co-locate. GuideClient는 탭 분기·렌더만 담당.
- 위험도: low
- 영향: -200줄, 새 AI 도구(예: Cursor) 추가 시 데이터만 추가하면 됨

---

## 4. 🟣 위치 위반

### 4-1. (관찰) `front/src/components/` 평면에 3개 도메인 그룹 혼재
- 증상: 23개 파일이 평면 구조. 도메인별 잠재 그룹:
  - **lecture**: GitHubGuide, MiniHompyGuide, VercelGuide, GitHubGuide.module.css, GuideClient (강의 교안 / 가이드)
  - **operator**: OperatorProfile, OperatorIntroCard, InstructorMicroCard, CurriculumSignature, DevOperatorEditor (운영자 5종)
  - **events**: EventCard, EventLocationMap, ClassEditor, ClassRegistration, OnedayClassCurriculum
  - **공통/전역**: Accordion, AdminOnly, Avatar, Footer, Header, ThemeToggle, HomeClient, LeafletMap
- 근거: `ls components/` → 23개 .jsx, 1개 .module.css.
- 권장: **현재 시점에서는 분리 X**. 글로벌 룰의 "domain-oriented decomposition은 size justifies일 때만". 25개는 평면 한계점 직전. 30개 넘기 전 또는 새 도메인(library, payment) 추가 시점에 `components/operator/`, `components/lecture/` 도입. 지금 도입하면 import 경로 25개 변경 비용이 이득보다 큼.
- 위험도: low
- 영향: 분리 시 import 경로 25곳 갱신

> component-placement.md 룰("2곳 이상에서 import → components/, 1곳만 → 사용처 폴더")은 **현재 모두 준수**. MiniHompy/MiniHompyLive는 오히려 모범 사례로 작동 중.

---

## 5. 🟢 데이터/스키마 정합성

### 5-1. `Home.jsx:7` — prop 이름 불일치 (실 버그)
- 증상: `<HomeClient events={events} site={siteData} />` 로 넘김. HomeClient는 `({ events, kakaoOpenChatUrl })` 로 받음. → **`site` prop 무시, `kakaoOpenChatUrl` 항상 undefined**.
- 근거: `Grep "kakaoOpenChatUrl"` → HomeClient 안에서 5군데 사용 (`<a href={kakaoOpenChatUrl}>` 3개, BeginnerGuide 카톡 버튼, CardsGrid empty 안내). 모두 `href={undefined}` → 브라우저는 같은 페이지로 리로드.
- 권장: `Home.jsx`를 `<HomeClient events={events} kakaoOpenChatUrl={siteData.kakaoOpenChatUrl} />` 로 수정 (1줄). **즉시 처리 권장.**
- 위험도: **high** (실 사용자 카톡 신청 흐름이 모두 깨짐)
- 영향: 홈 진입 사용자 100%

### 5-2. `front/public/data/curriculums.json` — 7개 필드 0회 참조
- 증상: 사용 0회 필드: `subtitle`, `summary`, `outline[]`, `outcomes[]`, `duration`, `level`, `guideRoute`, `demoComponent`. **사용 1회만**: `prerequisites[]` (ClassRegistration의 PrerequisitesBox).
- 근거: `Grep "curriculum\.outline|curriculum\.outcomes|curriculum\.summary|curriculum\.duration|curriculum\.subtitle|curriculum\.guideRoute|curriculum\.demoComponent" -> 0 hits`.
- 권장: **보존**. agents/frontend/class-registration-pattern.md에 "운영 시 사용 예정"으로 명시. EventDetail 또는 추후 회차 카드에서 outline/outcomes 노출 가능. 단 demoComponent는 사용 시점 명확치 않음 ("MiniHompyLive" 문자열 → 런타임 컴포넌트 매핑) — 도입 결정 필요.
- 위험도: low
- 영향: 데이터 ~50% 미활용. UI 가설 고정 시 노출 추가 권장.

### 5-3. `front/public/data/site.json` — 5개 필드 0회 참조
- 증상: 사용 0회: `subTagline`, `instagramUrl`, `danggeunUrl`, `nextEventId`, `refundPolicy[]`. (`tagline`은 OperatorIntroCard에서 `op.tagline`으로 참조 중 — 일치).
- 근거: `Grep "subTagline|instagramUrl|danggeunUrl|nextEventId|refundPolicy" front/src -> 0 hits`. `tagline` 사용은 `operator.tagline` (별도 객체).
- 권장:
  - `nextEventId` — class-registration-pattern.md에 "홈에서 다음 회차로 띄우기" 용도로 명시. 미구현. 활성 결정 필요.
  - `refundPolicy[]` — events.json의 `policies.refund` 와 중복(이중 진실 위험). **events 단위로 옮긴 후 site.json refundPolicy 제거 권장**.
  - `instagramUrl`, `danggeunUrl` — Footer는 `op.contacts.instagram` 사용. 중복.
  - `subTagline` — 미사용, 특정 페이지 가설 없으면 제거 가능.
- 위험도: low~med (refundPolicy 중복은 정합성 위험)
- 영향: 데이터 정합성

### 5-4. `front/public/data/{admin,board,qa}.json` — import 0회
- 증상: 3개 파일 모두 `front/src` 어디서도 import 없음. 페이지(`Admin.jsx`/community 페이지들)는 placeholder UI(`백엔드 연결 후 활성화 예정`).
- 근거: `Grep "admin\.json|board\.json|qa\.json" front/src -> 0 hits`.
- 권장: **보존** (Phase 2 백엔드 스키마 모킹용). Phase 2 진입 시 동일 스키마로 DB seed로 활용. 글로벌 룰 — 미래 placeholder 제거 X.
- 위험도: low
- 영향: 0

### 5-5. `events.json:54-67` (`evt-001`) — `host.handle` 필드 미사용
- 증상: 모든 event에 `host.name`은 EventDetail에서 표시되지만 `host.handle`은 어디서도 0회 참조. 정의: `"@vibe_session"` 같은 SNS 핸들.
- 근거: `Grep "host.handle|\.handle\b"` → 정의·blank만, 사용 0건.
- 권장: 보존. 향후 SNS 자동 멘션 등 활용 가능. ClassEditor `blankEvent`도 handle 자동 채움 — 의도적 구조.
- 위험도: low
- 영향: 0

### 5-6. `events.json` — `evt-001 ~ evt-111` 14건 중 13건이 `isPublished: false`
- 증상: 14건 중 단 1건(`evt-week1-2026-05-10`)만 게시. 나머지는 데모/외부 모킹용.
- 근거: events.json line 50, 70, 90, 110, ... 모두 `false`.
- 권장: **보존** (Phase 1 Admin 보조 뷰 / 미게시 표 / 외부 카드 데모용). 단 정리한다면 `events.json` 분할 (`events-published.json` + `events-fixtures.json`)도 가능. 현재는 useEvents 단일 소스 단순성이 더 중요.
- 위험도: low
- 영향: 0 (정상 동작)

---

## 6. ⚪ 문서 ↔ 코드 어긋남

### 6-1. `agents/ui/operator-profile-module.md` — `OperatorAboutHero` 컴포넌트가 존재하지 않음
- 증상: 문서의 4번 표·3번 props 표·About 섹션에 `OperatorAboutHero.jsx` 가 명시. **실제 `front/src/components/`에 파일 없음**. About.jsx는 `OperatorProfile`만 import. (단 git status untracked에 `?? front/src/components/OperatorAboutHero.jsx` 가 있을 가능성 — 분석 시점 파일 시스템에는 존재 X.)
- 근거: `Grep "OperatorAboutHero" front/src -> 0 hits`. `ls front/src/components/ | grep -i operator -> 3 files (OperatorIntroCard, OperatorProfile, DevOperatorEditor만)`.
- 권장: 둘 중 결정 필요 — (A) 문서대로 `OperatorAboutHero` 컴포넌트 신규 생성, (B) 문서에서 OperatorAboutHero 항목 제거하고 OperatorProfile variant prop으로 흡수. AI가 문서 따라 import 시도 → 빌드 실패 위험.
- 위험도: med
- 영향: AI 작업 시 잘못된 가정

### 6-2. `agents/ui/operator-profile-module.md` — `showAntiPlagiarismNotice` prop 0회 구현
- 증상: 문서가 `OperatorProfile`/`OperatorAboutHero` 의 prop으로 명시. **OperatorProfile.jsx는 이 prop 안 받음** (`{ variant, operator }` 만).
- 근거: `Grep "showAntiPlagiarismNotice" front/src -> 0 hits`. OperatorProfile 117줄 내 처리 로직 0개.
- 권장: 문서 갱신(prop 제거) 또는 코드 보강(prop 추가 + 안내 박스 렌더). 현재 About 페이지에 도용 방지 박스 0개 → 문서 의도가 미실현 상태.
- 위험도: low~med (도용 방지 시그니처 정책의 일부)
- 영향: 도용 방지 시그니처 누락

### 6-3. `agents/ui/theme-toggle-filter.md` — `html.light` 기준, 실제 코드는 `html.dark` 기준
- 증상: 문서 예시는 `html.light { filter: invert(...) }` (다크 디자인 → 라이트 토글). 실제 `globals.css`와 `ThemeToggle.jsx`는 `html.dark { filter: invert(...) }` (라이트 디자인 → 다크 토글) — 정반대 방향.
- 근거: globals.css line 13 `html.dark`. ThemeToggle.jsx line 14 `classList.toggle('dark', dark)`. 문서 line 17, 19, 65 모두 `light`.
- 권장: 문서를 `html.dark` 기준으로 갱신. 결과(filter invert + hue-rotate) 동일하므로 패턴 자체는 유효. 단 사용 시나리오만 반대.
- 위험도: low
- 영향: AI가 문서대로 라이트 모드 클래스 추가 시 충돌

### 6-4. `CLAUDE.md` 5절 — 배포 스크립트 경로 어긋남
- 증상: 문서 "front/server.ps1, front/server.py" 라고 적혀 있는데 **실제는 루트 `server.py` 단 1개**. `front/server.ps1`도 없음.
- 근거: `ls front/ | grep server -> 0 hits`. `ls / | grep server -> server.py only`.
- 권장: CLAUDE.md 5절을 "루트 `server.py` (cross-platform)" 로 수정 (1~2줄).
- 위험도: low
- 영향: AI가 잘못된 경로 시도

### 6-5. `agents/README.md` — 문서 누락
- 증상: README가 9개 문서를 인덱스. 그러나 `agents/frontend/` 실제 5개 (`admin-inline-editing-pattern.md` 포함됨 ✓). `agents/ui/` 실제 5개. CLAUDE.md 의 8개 표는 admin-inline 포함 → 일치. **agents/README.md는 admin-inline-editing-pattern.md를 frontend 표에 포함**(line 26). 일치 확인됨. 단 CLAUDE.md "전체 목록: agents/README.md" 라고 적혀 있는데 README와 CLAUDE.md 표가 한 번 더 동기화될 때마다 어긋날 잠재 리스크.
- 근거: 두 문서 cross-check. 현재 시점 일치.
- 권장: 일관성 위해 README가 단일 소스로 두고 CLAUDE.md에서는 README 링크만. 현재 CLAUDE.md가 8개 표를 직접 들고 있음 → 갱신 시 양쪽 동기화 필요.
- 위험도: low
- 영향: 미래 어긋남 가능성

---

## 7. 🔵 백엔드 부분

### 7-1. `back/` — Django 코드 작성됐으나 마이그레이션 0개 + 프론트 호출 0회
- 증상:
  - `apps/{accounts, events, library, community}/` 4개 앱: models, serializers, views, urls 모두 작성됨 (대략 13개 모델, 5개 ViewSet).
  - `apps/*/migrations/` — 모든 폴더가 `__init__.py` 만 있고 `0001_initial.py` 없음 → DB 스키마 미생성.
  - 프론트 `client/{auth,events,community}.js` — `apps/*/views.py`와 1:1 대응되는 인터페이스 정의됨 → **호출부 0회**.
  - `manage.py` 자동 venv 활성화 코드 있음. requirements.txt 있음.
- 근거: `ls back/apps/*/migrations/ -> 모두 __init__.py만`. `Grep "authClient|eventsClient|communityClient" front -> 정의만 hit, 호출 0`.
- 권장: **보존** (Phase 2 트리거 도달 시 활성). 글로벌 룰 — 명백한 placeholder 제거 X. 단 다음 검증 권장:
  - `back/apps/library/` — 프론트에는 `library` 라우트도 page도 client도 0개. **백엔드만 단독 정의된 유일한 도메인** → Phase 2 직전 다시 한 번 "정말 필요한가" 검토 필요. agents/docs는 library를 Phase 2 기능으로 명시.
  - `apps/events/services.py` 의 `register_event`/`confirm_payment`/`mark_attended` — 결제 관련. CLAUDE.md 3절 결제 정책 (Phase 2 PG 도입 트리거 도달 시까지 사이트에 박지 않음) 과 정합. 모델 작성된 채로 미사용 OK.
- 위험도: low (격리됨)
- 영향: 0 (실 호출 없음)

### 7-2. `back/config/settings/base.py:25` — `'apps.library'` INSTALLED_APPS에 등록
- 증상: 모델 1개(`LibraryItem`), 마이그레이션 0개. AUTH_USER_MODEL이 `accounts.Member` 라 library import 시점에 함께 로드됨.
- 근거: 위 7-1.
- 권장: 보존. apps.library 제거하면 `urls.py`의 `path('api/v1/library/', ...)` 도 함께 제거해야 하는 등 광범위 영향. Phase 2 결정 시 함께 결정.
- 위험도: low
- 영향: 0

---

## 우선순위 액션 플랜 (제안)

### 즉시 (low risk, high value, 5분 내)
1. **🔴 `Home.jsx` prop 수정** — `site={siteData}` → `kakaoOpenChatUrl={siteData.kakaoOpenChatUrl}`. 카톡 신청 흐름 복구. (5-1)
2. **🔴 routes/guide.jsx에 3개 라우트 추가** — Preview, beginner, claude. 페이지 코드 살아있음. (1-1)
3. **🔴 `<Link to="/library">` 결정** — guide로 변경 or 제거. (1-5)
4. **⚪ CLAUDE.md 5절 server.py 경로 수정** — front/server.* → 루트 server.py. (6-4)
5. **🟢 globals.css에 `btn`, `btn-ghost`, `section-title` 클래스 정의 추가** — 6회 사용 중인 디자인 의도 복원. (요약 Top 5)

### 다음 스프린트 (med risk, 30분~2시간)
6. **🟠 PromptRef 컴포넌트 추출** — components/PromptRef.jsx 단일 파일. (2-1)
7. **⚪ agents/ui/operator-profile-module.md 갱신** — OperatorAboutHero 항목 제거 또는 컴포넌트 신규 생성 결정. showAntiPlagiarismNotice prop도 같이. (6-1, 6-2)
8. **⚪ agents/ui/theme-toggle-filter.md 갱신** — html.dark 기준으로 수정. (6-3)
9. **🟢 site.json 정리** — `refundPolicy[]` 제거 (events.json policies.refund와 중복). `subTagline`/`instagramUrl`/`danggeunUrl` 제거 결정. (5-3)

### 장기 (Phase 2 직전 또는 새 Chapter 4개+ 만들 때)
10. **🟡 HomeClient.jsx 분리** — CalendarView → 별도 컴포넌트부터. 나머지 단계적. (3-1)
11. **🟡 GuideClient.jsx 데이터 분리** — STEPS_* → install-steps.js. (3-4)
12. **🟠 GitHubGuide/MiniHompyGuide/VercelGuide 통합** — `components/lecture/ChapterGuide.jsx` 1개 + 데이터 외부화. (2-2)
13. **🔵 백엔드 활성화 결정** — Phase 2 트리거 도달 시 client/* 호출부 추가, 마이그레이션 생성, library 도입 여부 결정. (7-1, 7-2)
14. **🟣 components/ 도메인 폴더 분리** — 30개 넘기 직전에. (4-1)

---

## 결정 필요 항목 (사용자 확인 후 진행)

| # | 결정 사항 | 옵션 |
|---|----------|-----|
| A | `/library` 라우트 — 도입 vs 폐기 | (A) 라우트 + page 추가 (Phase 2급), (B) 홈 CTA 버튼 제거 |
| B | `OperatorAboutHero` — 신규 생성 vs 문서에서 제거 | (A) 컴포넌트 만들고 About 최상단 마운트, (B) OperatorProfile variant로 흡수 후 문서 정리 |
| C | `showAntiPlagiarismNotice` prop — 구현 vs 문서 정리 | (A) OperatorProfile에 prop 추가 + About에서 true, (B) 문서에서 prop 항목 제거 |
| D | `curriculum.outline/outcomes/duration/...` 7개 필드 — 노출 vs 제거 | (A) EventDetail에 outline/outcomes 노출 추가, (B) 데이터에서 삭제 (사용 가설 미정) |
| E | `nextEventId` (site.json) — 활용 vs 제거 | (A) 홈 RecommendedHero가 이 필드 우선 참조, (B) 제거 (현재는 시간순 정렬로 자동 선정 중) |
| F | `back/apps/library/` — 유지 vs 제거 | (A) 유지 (Phase 2 활성), (B) 제거 (라이브러리 기능 보류 결정 시) |
| G | `events.json` 분할 — `events.json` (게시) + `events-fixtures.json` (외부 모킹) | 단일 파일 유지 vs 분할 |
| H | `client/*` axios 모듈 — 보존 vs 제거 | 글로벌 룰상 보존 권장. 단 axios 의존성 ~13kb 부담. |

---

## 부록 — 검사 카운트 메모 (재현용 grep)

```
# import 0회 export
Grep "AdminOnly\b" front/src               → AdminOnly default 0 호출
Grep "DevOnly" front/src                   → 정의 1, 호출 0
Grep "\bformatDate\b" front/src            → 0
Grep "libraryFormatLabel" front/src        → 0
Grep "authClient|eventsClient|communityClient" → 정의만, 호출 0
Grep "OperatorAboutHero" front/src         → 0 (파일도 없음)
Grep "showAntiPlagiarismNotice" front/src  → 0

# 데드 라우트
Grep "/library" front/src                  → HomeClient line 360 only
Grep "guide/oneday/preview" front/src      → 표시·링크 7건, route 정의 0

# 미사용 JSON 필드
Grep "curriculum\.(outline|outcomes|summary|duration|subtitle|guideRoute|demoComponent)" → 0
Grep "subTagline|instagramUrl|danggeunUrl|nextEventId|refundPolicy" front/src → 0
Grep "admin\.json|board\.json|qa\.json" front/src → 0

# 중복
Grep "function PromptRef|const PromptRef" → 정의 2 (MiniHompyGuide, VercelGuide)

# 비대 파일 (wc -l)
HomeClient.jsx 624, GuideClient.jsx 492, OnedayClassCurriculum.jsx 375,
ClassEditor.jsx 348, VercelGuide.jsx 291, DevOperatorEditor.jsx 243,
GitHubGuide.jsx 237, MiniHompyGuide.jsx 212, Admin.jsx 197, ClassRegistration.jsx 188

# 미정의 CSS 클래스
Grep "btn-ghost" → HomeClient line 575, 577 (정의 0)
Grep "section-title" → HomeClient line 339 (정의 0)
Grep "\bbtn\b" 단독 → HomeClient line 357, 360, 539; DevOperatorEditor line 194, 201 (정의 0)

# 백엔드 placeholder
ls back/apps/*/migrations/ → __init__.py만, 0001_initial.py 0개
```
