# 홈 레이아웃 재구성 — 옵션 F 적용 (강의·교안·지도·접힘 캘린더)

작업일: 2026-05-05
선행: `docs/plan/2026-05-05/1400_홈_레이아웃_확정안_강의_교안_캘린더.md` (확정안)
정체성: **강의 + 교안 + 위치·일정** (모임 모집/외부 카탈로그는 사이트 밖)

---

## 왜 이렇게 했나

1330 → 1400 의 의사결정 fork 거쳐 확정한 안:
- 사이트 = 강의·교안·신청 안내 + 위치·일정 정보
- 모임 모집·운영 = 소모임/카톡 (외부)
- 외부 모임 카탈로그 = 정체성 안 맞음 → 데이터까지 삭제
- 지도 = 메인 유지 ("강의 위치가 우리집과 가까운지" 운영자 instinct)
- 캘린더 = 자료 적은 단계라 큰 빈 면적 — Accordion 접힘 기본

## 수정 파일

### 데이터
| 파일 | 변경 |
|------|------|
| `front/src/data/events.json` | evt-101 ~ evt-111 외부 11건 데이터 삭제. 자체 4건(week1 + 001/002/003 미게시 더미)만 남김. **339 → 108 줄** |
| `front/public/data/events.json` | 동일 (src 와 동기화) |

### 컴포넌트
| 파일 | 변경 |
|------|------|
| `front/src/components/NextClassHero.jsx` | **신규** — 큰 좌-우 분할 hero 카드. 가장 가까운 자체 강의 1건. 좌측 썸네일 16:10, 우측 제목·일시·장소·가격·잔여석·신청 CTA |
| `front/src/components/HomeClient.jsx` | **640줄 → 220줄 슬림화.** 아래 § 영역별 변경 참조 |

### 보존 (dead 후보지만 향후 재사용 예정)
- `EventCard.jsx` — 자체 강의 회차 N개로 늘 때 카드 그리드 부활 가능. import 0건이지만 의도적 보존
- `MiniHompy`, `MiniHompyDemo` — 가이드 영역(GuideClient, MiniHompyGuide, Demo.jsx)에서 사용 중
- 외부 이벤트 처리 로직 일부 — admin 시스템에서 향후 외부 추가 가능성

## 영역별 변경 (HomeClient)

### 제거
- 자체/외부 토글 (`sourceFilter` state)
- 필터 패널 (region/type/price + 상단 칩)
- 날짜 드롭다운 (`dateFilter` + DATE_FILTERS 상수)
- 상단 카드 3종 grid (운영자 명함 + GuidePreviewCard + RecommendedHero)
- 우측 sticky aside (지도 + BeginnerGuide compact)
- "후기" 섹션 (TESTIMONIALS) — 1차 강의 후 진짜 후기로 채울 자리. placeholder 가 본인 톤("거창하지 않은") 어긋남
- 검은 CTA 박스 (구경하러 왔어요)
- 지도 모바일 아코디언 (지도가 단독 섹션으로 격상)
- 캘린더 모달 (`calModal` state + 모달 div) — Accordion 접힘 인라인으로 교체
- `recommended` 로직 — `nextClass` (가장 가까운 미래 자체 강의 1건) 으로 단순화
- `today` 하드코딩 (`'2026-04-25T12:00:00+09:00'`) → `new Date()` 실제 시간

### 신규 / 위치 변경
- HERO + 운영자 명함 영역: `max-w-3xl mx-auto` 좁게
- 메인 콘텐츠: `max-w-5xl mx-auto` 중간폭. 5섹션 세로 stack
  1. 다음 강의 hero (NextClassHero)
  2. 지도 (단독, 자체 강의 마커만)
  3. 강의 교안 4주차 (CurriculumGrid 재사용)
  4. 캘린더 (Accordion `defaultOpen={false}` + `alwaysOpenOnDesktop={false}` — 데스크탑도 접힘)
  5. admin only "+ 새 강의" CTA
- 처음이세요? + 카톡 CTA: `max-w-3xl mx-auto` 좁게 회귀

### 보존
- `CalendarView` 인라인 함수 (모달 → 인라인 위치 이동)
- `dynamic` lazy import (`KakaoMultiMap`)
- `Accordion` 재사용 (캘린더 토글)

## CLAUDE.md 룰 예외

`§ 1-5-2` "데스크탑(`lg:`)에서는 항상 펼친 상태 유지" 의 **예외**:
- 캘린더는 자료 적은 단계에서 큰 빈 면적이라 데스크탑에서도 접힘 기본
- `Accordion alwaysOpenOnDesktop={false}` 로 명시
- 룰 자체는 일반 가이드. 캘린더가 그 예외 케이스 (한 컴포넌트 한정)

## 검증 (운영자 직접)

dev 서버에서 확인 필요:
- [ ] `/` 진입 → 새 레이아웃 (좁게-넓게-좁게) 정상 렌더
- [ ] 모바일 360~430px (iPhone XR) → 모든 섹션 깨짐 없음
- [ ] 데스크탑 1280px → max-w-3xl/5xl 좌우 여백 OK
- [ ] 캘린더 헤더 클릭 → 펼침/접힘 토글 작동
- [ ] 지도에 5/10 서초 마커 1개 표시
- [ ] 다음 강의 hero 카드 클릭 → `/events/evt-week1-2026-05-10` 진입
- [ ] CurriculumGrid 4장 (1주차 진행중 + 2~4 예정) 표시
- [ ] admin 모드 → "+ 새 강의" CTA 노출
- [ ] 비-admin → "+ 새 강의" 안 보임

## 안 한 것 / 보류

- **EventCard 정리** — 자체 강의 N회차 부활 시 재사용 예정. 보존
- **외부 모임 시스템 전체 제거** — events.json 의 source/externalSource 필드는 보존 (admin 폼이 외부 추가도 지원). 운영 정체성 변경되어도 코드 재작성 비용 0
- **헤더 "모임" 라벨** — 그대로 유지 (사용자 결정 안 줌)
- **evt-001/002/003 미게시 더미** — 보존 (사용자 결정 안 줌)
- **후기 영역** — 1차 강의 후 진짜 후기 1~2건 받으면 부활. 코드 통째 제거 (git history 에서 부활 가능)
- **지식 글 영역** — 1차 강의 후 결정 (1400 § 1)

## 다음 단계 후보

- 1차 강의 (5/10) 진행 → 후기 영역 부활 (실제 후기 받기)
- 2주차 교안 본문 작성 (`Week2/index.jsx` 채우기)
- 운영자 본인이 admin 으로 evt-week1-2026-05-10 의 payment.bank/account TODO 채우기
- OG 이미지를 강의 후 진짜 사진으로 교체
